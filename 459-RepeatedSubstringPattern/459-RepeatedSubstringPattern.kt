            } else {
                return true
            }
                windowEnd++
            if(s.split(s.substring(0..windowEnd)).any { it.isNotEmpty() }){
        while (windowEnd < s.lastIndex) {
        var windowEnd = 1
        if (s.length <= 1) return false
    fun repeatedSubstringPattern(s: String): Boolean {
class Solution {
        if (s.toSet().size == 1) return true
"
