        }
        return letters.first()
    }

    private fun binarySearch(arr: CharArray, target: Char, start: Int, end: Int): Int {
        if (start > end) return -1
        val size = end - start + 1
        val middle = start + size / 2
        return when {
                return letters[result]
            if(result != -1)
            result = binarySearch(letters, it, 0, letters.lastIndex)
        (target.plus(1)..'z').forEach {
        var result = -1
class Solution {
    fun nextGreatestLetter(letters: CharArray, target: Char): Char {
[
