        var start = 0
    private fun binarySearch(array: String): Char {

    }
       var num  = 0
    fun minPartitions(n: String): Int {
class Solution {
            n.forEach {
                if (it - '0' > num) num = it - '0'
            }
        return num
"
