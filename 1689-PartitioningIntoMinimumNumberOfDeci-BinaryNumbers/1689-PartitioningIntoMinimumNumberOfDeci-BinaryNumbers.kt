        var end = array.lastIndex
        while (start < end) {
            val middle = start + (end - start) / 2
            when {
        var start = 0
    private fun binarySearch(array: String): Char {
    }

        return binarySearch(n.toCharArray().sorted().joinToString("")).digitToInt()
    fun minPartitions(n: String): Int {
class Solution {
"
