            if (double.length > longest.length) {

            val double = expand(i, i + 1)
            val single = expand(i - 1, i + 1)
        }

        for (i in 0..<s.length) {
            return expand(left - 1, right + 1)

            }
                return s.substring(left + 1, right)
            if (left < 0 || right >= s.length || s[left] != s[right]) {
        fun expand(left: Int, right: Int): String {

        var longest = ""
fun longestPalindrome(s: String): String {
class Solution {
"
